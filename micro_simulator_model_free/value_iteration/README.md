# Value iteration

```bash
cd micro_simulator_model_free
python -m value_iteration.main
```

Map setup → step or run VI → **Change world**.

States \((r,c,h)\); actions straight / turn R / L / A. Default \(\gamma=0.85\).

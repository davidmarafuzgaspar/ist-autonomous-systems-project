# Value iteration (forward slip)

```bash
cd micro_simulator_model_free
python -m value_iteration_non_deterministic.main
```

Same workflow as [`value_iteration/`](../value_iteration/README.md). Forward slip default 70% / 15% / 15% (intended / left / right); edit in the viewer.
